# System Requirements

VITA relies on Kokoro TTS which uses **espeak-ng** for phoneme processing on some platforms.

### Linux
    :::python
    sudo apt-get update
    sudo apt-get install espeak-ng

### Windows
    
    Download espeak-ng from Releases

    Run the installer (espeak-ng-setup.exe)

    Add it to your System PATH



---

